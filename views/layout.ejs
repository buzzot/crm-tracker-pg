<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= typeof title !== 'undefined' ? title + ' · CRM' : 'CRM' %></title>
  <link rel="icon" type="image/png" href="https://samyouusa.com/wp-content/uploads/2025/12/cropped-ABUIABAEGAAggq-etQYomuf-ygMwghM40As-270x270.png">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Instrument+Serif:ital@0;1&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
  <link href="/css/style.css" rel="stylesheet">
</head>
<body>
  <nav class="navbar navbar-expand-lg app-navbar">
    <div class="container-fluid">
      <a class="navbar-brand fw-semibold d-flex align-items-center" href="/">
        <span class="brand-icon me-2"><i class="bi bi-stars"></i></span>Samyou <span class="brand-tag ms-1">CRM</span>
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navMenu">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navMenu">
        <ul class="navbar-nav me-auto app-nav-pills" id="appNavPills">
          <li class="nav-item"><a class="nav-link" href="/"><i class="bi bi-speedometer2"></i><span class="nav-label">Dashboard</span></a></li>
          <li class="nav-item"><a class="nav-link" href="/pipeline"><i class="bi bi-funnel"></i><span class="nav-label">Pipeline</span></a></li>
          <li class="nav-item"><a class="nav-link" href="/activities"><i class="bi bi-activity"></i><span class="nav-label">Activity</span></a></li>
          <li class="nav-item"><a class="nav-link" href="/companies"><i class="bi bi-building"></i><span class="nav-label">Companies</span></a></li>
          <li class="nav-item"><a class="nav-link" href="/projects"><i class="bi bi-folder2-open"></i><span class="nav-label">Projects</span></a></li>
          <li class="nav-item"><a class="nav-link" href="/tasks"><i class="bi bi-list-check"></i><span class="nav-label">Tasks</span></a></li>
          <li class="nav-item"><a class="nav-link" href="/products"><i class="bi bi-box-seam"></i><span class="nav-label">Products</span></a></li>
          <% if (currentUser && currentUser.role === 'Admin') { %>
            <li class="nav-item"><a class="nav-link" href="/admin"><i class="bi bi-gear"></i><span class="nav-label">Admin</span></a></li>
          <% } %>
        </ul>
        <% if (currentUser) { %>
        <div class="d-flex align-items-center gap-2">
          <a href="/profile" class="user-chip user-chip-link" title="My profile">
            <%
              const _navColor = currentUser.avatarColor;
              const _navColorMap = { indigo:'oklch(0.50 0.20 270)', blue:'oklch(0.52 0.18 250)', teal:'oklch(0.52 0.14 195)', emerald:'oklch(0.52 0.16 160)', amber:'oklch(0.62 0.18 72)', rose:'oklch(0.52 0.20 10)', slate:'oklch(0.35 0.02 265)' };
              const _navBg = _navColorMap[_navColor] || null;
            %>
            <span class="user-avatar" <% if (_navBg) { %>style="background:<%- _navBg %>"<% } %>><%= (currentUser.name || currentUser.email || '?').charAt(0).toUpperCase() %></span>
            <span class="d-none d-sm-inline"><%= currentUser.name || currentUser.email %></span>
            <span class="badge text-bg-light text-dark ms-1"><%= currentUser.role %></span>
          </a>
          <form action="/logout" method="POST" style="margin:0;">
            <button class="btn btn-sm btn-outline-secondary" type="submit">Log out</button>
          </form>
        </div>
        <% } %>
      </div>
    </div>
  </nav>

  <main class="container-fluid py-4">
    <%- body %>
  </main>

  <!-- Activity / Project Activity detail modal (populated via AJAX) -->
  <div class="modal fade" id="activityModal" tabindex="-1">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="activityModalTitle">Activity</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
        </div>
        <div class="modal-body" id="activityModalBody">
          <div class="text-muted">Loading...</div>
        </div>
      </div>
    </div>
  </div>

  <!-- Email preview modal: live, read-only fetch over IMAP — never persisted -->
  <div class="modal fade" id="emailPreviewModal" tabindex="-1">
    <div class="modal-dialog modal-lg">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title">Email preview</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
        </div>
        <div class="modal-body" id="emailPreviewBody">
          <div class="text-muted">Loading...</div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/app.js"></script>
  <script>
    (function(){
      var path = window.location.pathname;
      document.querySelectorAll('#appNavPills .nav-link').forEach(function(a){
        var href = a.getAttribute('href');
        var isActive = href === '/' ? path === '/' : path === href || path.startsWith(href + '/');
        if (isActive) a.classList.add('active');
      });
    })();
  </script>
</body>
</html>
